# v7.0 Git 브랜치 전략 및 워크플로우

## 🌳 브랜치 구조 개요

### 1. 메인 브랜치
- **`main`**: 
  - 프로덕션 준비 완료 코드 (안정 버전)
  - **보호**: 직접 푸시 제한. Pull Request (PR)를 통해서만 병합
  - **태깅**: 릴리스 버전 (예: `v7.0.0`)이 여기에 태그됨

- **`develop`**: 
  - 다음 릴리스를 위한 통합 브랜치
  - 모든 기능이 테스트를 위해 먼저 여기에 병합됨

### 2. 기능 브랜치 (v7.0)
v7.0 Phase를 위한 계층적 기능 브랜치 전략을 따릅니다.

- **Phase 브랜치**: `feature/v7-phase{N}-{name}`
  - 예시: `feature/v7-phase1-agent`
  - 생성 기준: `develop`
  - 병합 대상: `develop` (Phase 완료 시)

- **작업 브랜치**: `feature/issue-{ID}-{description}` 또는 `fix/issue-{ID}-{description}`
  - 예시: `feature/issue-1-langgraph-setup`
  - 생성 기준: **Phase 브랜치** (예: `feature/v7-phase1-agent`)
  - 병합 대상: **Phase 브랜치**

---

## 🔄 워크플로우 시나리오 (Phase 1 예시)

### Step 1: Phase 브랜치 생성
`develop`에서 전용 브랜치를 생성하여 Phase 1 작업을 시작합니다.
```bash
git checkout develop
git pull origin develop
git checkout -b feature/v7-phase1-agent
```

### Step 2: Issue #1 작업 (LangGraph 설정)
특정 이슈를 위한 작업 브랜치를 생성합니다.
```bash
# Phase 브랜치에 있는지 확인
git checkout feature/v7-phase1-agent
git checkout -b feature/issue-1-langgraph-setup
```
... (작업, 커밋) ...

### Step 3: 작업 → Phase 병합
작업이 완료되면 **Phase 브랜치**를 대상으로 PR을 엽니다.
- **소스**: `feature/issue-1-langgraph-setup`
- **타겟**: `feature/v7-phase1-agent`
- **리뷰**: 셀프 리뷰 또는 동료 리뷰

### Step 4: Phase → Develop 병합
Phase 1의 모든 작업이 완료되고 Phase 브랜치에서 테스트되면:
1. `develop`을 대상으로 PR을 엽니다.
   - **소스**: `feature/v7-phase1-agent`
   - **타겟**: `develop`
2. 통합 테스트를 수행합니다.
3. 병합하고 Phase 브랜치를 삭제합니다.

---

## 🏷 네이밍 규칙

| 유형 | 접두사 | 형식 | 예시 |
|---|---|---|---|
| 새 기능 | `feature/` | `feature/issue-{ID}-{desc}` | `feature/issue-1-agent-setup` |
| Phase 루트 | `feature/` | `feature/v7-phase{N}-{name}` | `feature/v7-phase1-agent` |
| 버그 수정 | `fix/` | `fix/issue-{ID}-{desc}` | `fix/issue-5-typo-fix` |
| 문서화 | `docs/` | `docs/issue-{ID}-{desc}` | `docs/issue-10-update-readme` |

---

## 🚀 치트 시트

```bash
# 1. v7.0 Phase 1 시작
git checkout develop
git checkout -b feature/v7-phase1-agent

# 2. 작업 시작 (Issue #1)
git checkout -b feature/issue-1-langgraph-setup

# 3. 작업 완료
git add .
git commit -m "feat [#1]: 🤖 setup langgraph infrastructure"
git push origin feature/issue-1-langgraph-setup
# → feature/v7-phase1-agent로 PR 생성
```
