# v7.0 작업 목록 및 GitHub 이슈 계획

이 문서는 v7.0 개발을 추적하기 위해 생성할 구체적인 GitHub 이슈를 정리합니다.

---

## 📦 Phase 1: 고급 AI 에이전트 (LangGraph)

### Issue #1: [Feat] LangGraph 기반 에이전트 워크플로우 설계 및 도입
- **설명**:
  - 기존 `Classifier` 로직을 **LangGraph**의 `StateGraph`로 리팩토링합니다.
  - 단일 LLM 호출을 **노드(Node)** 단위로 분리 (예: `InputAnalysis` → `ContextRetrieval` → `Classification` → `OutputParsing`)
- **작업 항목**:
  - [ ] `langgraph` 라이브러리 설치 및 설정
  - [ ] 기본 `State` 스키마 정의 (`messages`, `current_doc`, `classification_result`)
  - [ ] `ClassifierAgent` 클래스 구현

### Issue #2: [Feat] Self-Correction (자가 수정) 로직 구현
- **설명**:
  - 분류 신뢰도(Confidence Score)가 임계값(예: 70%) 미만일 경우, 에이전트가 스스로 재검토하는 루프(Loop)를 구현합니다.
- **작업 항목**:
  - [ ] `ValidationNode` 추가: 신뢰도 검사
  - [ ] `ReflectionNode` 추가: 낮은 신뢰도의 원인 분석 및 재시도 프롬프트 생성
  - [ ] Conditional Edge 설정 (신뢰도 높음 → 종료, 낮음 → 재시도)

### Issue #3: [Feat] Context-Aware Memory (Redis 연동)
- **설명**:
  - 에이전트가 이전 분류 이력과 사용자 피드백을 기억하도록 **단기 메모리**를 구현합니다.
- **작업 항목**:
  - [ ] Redis 기반 `CheckpointSaver` 설정 (LangGraph Persistence)
  - [ ] 사용자 피드백(수동 분류 변경)을 학습/저장하는 로직 추가
  - [ ] 분류 시 이전 유사 사례 참조 기능 구현

---

## 📦 Phase 2: 지능형 자동화 (RAG & 채팅)

### Issue #4: [Feat] RAG 파이프라인 고도화
- **설명**:
  - '문서와 대화하기' 기능을 위한 고성능 RAG 파이프라인을 구축합니다.
- **작업 항목**:
  - [ ] 문서 청킹(Chunking) 전략 최적화 (RecursiveCharacterTextSplitter)
  - [ ] 하이브리드 검색 구현 (키워드 + 벡터)
  - [ ] 메타데이터 필터링 기능 추가 (PARA 카테고리별 검색)

### Issue #5: [UI] 'Chat with Docs' 인터페이스 구현
- **설명**:
  - 대시보드 내에 AI 챗봇 위젯을 구현하고, 스트리밍 응답을 지원합니다.
- **작업 항목**:
  - [ ] Vercel AI SDK (`useChat`) 도입
  - [ ] ShadCN UI 기반 채팅 컴포넌트 개발
  - [ ] Markdown 렌더링 및 출처 표기 기능

---

## 📦 Phase 3: 멀티 플랫폼 통합

### Issue #6: [Feat] Slack Bot 통합
- **설명**:
  - Slack 채널에서 FlowNote 에이전트와 상호작용할 수 있는 봇을 개발합니다.
- **작업 항목**:
  - [ ] Slack Bolt for Python 설정
  - [ ] 파일 업로드 이벤트 핸들러 (Slack → FlowNote)
  - [ ] `/ask` 슬래시 커맨드 구현 (FlowNote 검색)

### Issue #7: [Feat] 이메일 수집 시스템
- **설명**:
  - 특정 이메일 주소로 문서를 전송하면 자동으로 분류 및 저장하는 파이프라인을 구축합니다.
- **작업 항목**:
  - [ ] IMAP 클라이언트 구현 (이메일 수신/파싱)
  - [ ] 첨부파일 추출 및 업로드 프로세스 자동화
  - [ ] 처리 결과 이메일 회신 자동화

---

**다음 단계**:
1. 이 목록을 검토합니다.
2. 이 템플릿을 사용하여 GitHub에 이슈를 생성합니다.
3. Issue #1 (LangGraph 설정)부터 시작합니다.
